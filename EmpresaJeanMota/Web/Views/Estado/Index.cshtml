@model IEnumerable<Dominio.Estado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Novo Cadastro", "Cadastrar")
</p>
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.IdEstado)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.NomeEstado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        @*<td>
            @Html.DisplayFor(modelItem => item.IdEstado)
        </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.NomeEstado)
        </td>
        <td>
            @Html.ActionLink("Editar", "Editar", new { id=item.IdEstado }) |
            @Html.ActionLink("Detalhes", "Detalhes", new { id=item.IdEstado }) |
            @Html.ActionLink("Excluir", "Excluir", new { id=item.IdEstado })
        </td>
    </tr>
}

</table>
